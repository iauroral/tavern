import { CateringSendPipe } from './catering-send.pipe';

describe('CateringSendPipe', () => {
  it('create an instance', () => {
    const pipe = new CateringSendPipe();
    expect(pipe).toBeTruthy();
  });
});
