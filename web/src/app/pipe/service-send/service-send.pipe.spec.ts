import { ServiceSendPipe } from './service-send.pipe';

describe('ServiceSendPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceSendPipe();
    expect(pipe).toBeTruthy();
  });
});
